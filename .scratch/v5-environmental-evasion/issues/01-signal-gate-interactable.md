# 01 — Signal Gate Interactable Node

**What to build:**
`SignalGateInteractable : InteractableBase` node at `Vector3(-1.5, 0.5, 12.0)` with signal light and interaction trigger.

**Blocked by:** None — frontier ticket.

**Status:** ready-for-agent

- [ ] `SignalGateInteractable` node extending `InteractableBase`
- [ ] Positioned at `Vector3(-1.5, 0.5, 12.0)`
- [ ] Signal light indicator (Cyan = ready, Red = triggered)
- [ ] Interaction trigger API method (`trigger_gate()`)
